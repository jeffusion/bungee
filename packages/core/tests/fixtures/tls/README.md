# TEST ONLY

`managed-upstream.crt.pem` and `managed-upstream.key.pem` are self-signed fixtures
used only by the real Master/plugin-control integration test. They must not be
used by production or release tooling.
